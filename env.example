# Environment Variables Template
# Copy this file to ~/.env and fill in your actual values
#
# Usage:
#   cp ~/dotfiles/env.example ~/.env
#   nano ~/.env  # Edit with your actual credentials
#
# Security Note:
#   - ~/.env is NOT tracked by git (loaded by .bashrc)
#   - Never commit real credentials to version control
#   - Keep this file updated when adding new variables

# ====================
# Git Configuration
# ====================

# GitLab Access Token
# 用於 HTTP clone/push 操作
# 產生位置: https://gitlab.syntecclub.com/-/profile/personal_access_tokens
# 權限需求: read_repository, write_repository
export GITLAB_TOKEN="glpat-your_token_here"

# ====================
# MCP Servers
# ====================

# SYNTEC Confluence/JIRA MCP
# Email 和 API Token 從 Atlassian 帳號取得
# API Token: https://id.atlassian.com/manage-profile/security/api-tokens
export SYNTEC_EMAIL="your.email@syntecclub.com.tw"
export SYNTEC_API_TOKEN="your_atlassian_api_token_here"

# Notion MCP
# Token 從 Notion Integration 取得
# https://www.notion.so/my-integrations
export NOTION_TOKEN="ntn_your_token_here"

# Context7 MCP
# API Key 從 Upstash Console 取得
# https://console.upstash.com/
export CONTEXT7_API_KEY="ctx7sk-your_api_key_here"

# ====================
# Hardware Control
# ====================

# Tasmota 智能插座設定（用於 CNC 控制器重置）
# IP 位址（透過 Windows PC 網路存取）
export TASMOTA_IP="192.168.x.x"

# ====================
# Z.AI GLM API
# ====================

# GLM API Key from Z.AI Open Platform
# 取得位置: https://api.z.ai (API Keys management page)
# 使用方式: claude-glm 命令會自動使用此 API key
export GLM_API_KEY="your-api-key-here"

# ====================
# Custom Variables
# ====================

# Add your custom environment variables below

# ====================
# Windows PC Connection
# ====================

# Windows PC SSH credentials
export WINDOWS_HOST="windows_pc"
export WINDOWS_PASSWORD="your_windows_ssh_password"

# ====================
# Appkernel Build System
# ====================

# Appkernel uses Windows PC credentials (reference to above)
export APPKERNEL_WINDOWS_HOST="${WINDOWS_HOST}"
export APPKERNEL_WINDOWS_PASSWORD="${WINDOWS_PASSWORD}"

# Samba credentials (for mounting network share)
export APPKERNEL_SAMBA_USER="your_username"
export APPKERNEL_SAMBA_PASSWORD="your_samba_password"

# ====================
# Native Build System (C++ Projects)
# ====================

# OCUser project location
export OCUSER_SOURCE_DIR="${HOME}/path/to/ocuser/source"

# Cross-compiler SDK paths (請根據實際安裝路徑設定)
export IMX8_SDK_PATH="/path/to/imx8/sdk"
export AM625_SDK_PATH="/path/to/am625/sdk"

# Native library deployment path on controller
export CNC_NATIVE_LIB_PATH="/path/to/controller/lib"

# Delta Spec: glm-dynamic-model-display

## REMOVED Requirements

### Requirement: statusline 動態解析 GLM model 名稱

**Reason**: `claude-code-statusline` 為 Claude Code statusline 腳本，隨 claude CLI 退役刪除。

**Migration**: 無 repo 內承接。opencode 端 model 顯示由 opencode 本體與 `@slkiser/opencode-quota` plugin 承擔。

### Requirement: statusline 前綴格式簡化

**Reason**: 同上 — 規範對象 `claude-code-statusline` 刪除。

**Migration**: 無承接。

# Delta Spec: tmux-window-rename

## REMOVED Requirements

### Requirement: 啟動時重新命名 tmux window

**Reason**: 規範 `claude-glm` wrapper 啟動 `claude` 前的 tmux window 改名行為；`claude-glm` 隨 claude CLI 退役刪除。opencode 不透過 wrapper 啟動，無對應行為。

**Migration**: 無承接。

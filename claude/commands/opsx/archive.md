---
name: "OPSX: Archive"
description: Archive a completed change in the experimental workflow
category: Workflow
tags: [workflow, archive, experimental]
---

Archive a completed change, including delta-spec sync assessment.

**Input**: Optionally a change name (e.g., `/opsx:archive add-auth`). If omitted, infer from conversation context; if ambiguous, prompt for selection.

Use the `openspec-archive-change` skill to run this workflow.
